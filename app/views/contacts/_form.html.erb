<%= form_for @contact do |f| %>
  <%= f.error_messages %>
  <table>
    <tr>
      <td>
      <table>
        <tr>
          <td>
            <%= f.label :email %><br />
            <%= f.text_field :email %><br>
          </td>
        </tr>
        <tr>
          <td colspan="2">
            <%= f.label :comments, "Your feedback" %><br />
            <%= f.text_area :comments, :cols => 50, :rows => 8 %>
          </td>
        </tr>
        <tr>
          <td colspan="2">
            <%= f.submit "Send" %>
          </td>
        </tr>
      </table> 
  
      </td>
    </tr>
  </table>

<% end %>

<%= javascript_tag do %>
  $('#contact_email').select();
<% end %>